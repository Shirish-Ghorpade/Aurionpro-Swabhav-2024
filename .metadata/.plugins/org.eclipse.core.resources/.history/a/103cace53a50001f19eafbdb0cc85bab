package com.aurionpro.structural.composite.model;

import java.util.ArrayList;
import java.util.List;

public class Directory implements FileSystem{
	
	private String directoryName;
	private List<FileSystem> fileSystemsList = new ArrayList<FileSystem>();

	public Directory(String directoryName, List<FileSystem> fileSystemsList) {
		super();
		this.directoryName = directoryName;
		this.fileSystemsList = fileSystemsList;
	}
	
	public void add(FileSystem fileSystemObj) {
		fileSystemsList.add(fileSystemObj);
		
	}

	@Override
	public void ls() {
		// TODO Auto-generated method stub
		System.out.println("Directory name is : "+directoryName);
		for(FileSystem fileSystem : fileSystemsList) {
			fileSystem.ls();
		}
	}
}
